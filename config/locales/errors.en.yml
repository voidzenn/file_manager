en:
  errors:
    params:
      missing:
        message: "Parameter missing"
      unpermitted:
        message: "Unpermitted Parameter"
    messages:
      required: "must exist"
      blank: "cannot be blank"
      invalid: "is invalid"
      taken: "already exists"
      not_a_number: "is not a number"
    unauthorized:
      default:
        message: "Unauthorized"
      invalid_email_password:
        message: "Email or Password is invalid"
    internal_server_error:
      default:
        message: "Internal Server Error"
    models:
      folder:
        format:
          message: "should only contain alphanumeric characters, spaces, underscores and hyphens for the name, should end with a forward slash."
        uniqueness:
          message: "already exists"
        same_as_previous_path_name:
          message: "should not be the same as previous path name"
